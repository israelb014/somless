// ---------------------------------------------------------------------------
// קונפיגורציה מרכזית של האפליקציה.
// ה-MVP הוא משפחה אחת ואלרגן אחד, אבל כל הקוד עובר דרך הקבועים האלה
// כך שהרחבה לריבוי משפחות/אלרגנים לא תדרוש refactor.
// ---------------------------------------------------------------------------

/** מזהה המשפחה היחיד ב-MVP (זהה ל-FAMILY_ID של סקריפט ה-seed). */
export const FAMILY_ID = import.meta.env.VITE_FAMILY_ID || 'family-main'

/** האלרגן הפעיל ב-UI. מודל הנתונים תומך במפה של אלרגנים. */
export const ACTIVE_ALLERGEN = 'sesame'

/** כל האלרגנים המוכרים למודל הנתונים (ב-UI מוצג רק הפעיל). */
export const ALLERGENS = {
  sesame: { id: 'sesame', label: 'שומשום' },
}

/** מצבי הסטטוס האפשריים, כולל צבע וטקסט — מקור אמת יחיד לכל האפליקציה. */
export const STATUSES = {
  contains: {
    id: 'contains',
    label: 'מכיל שומשום',
    shortLabel: 'מכיל',
    color: '#EF4444',
    tint: 'rgba(239, 68, 68, 0.14)',
    hint: 'לא לתת לתינוק.',
  },
  may_contain: {
    id: 'may_contain',
    label: 'עלול להכיל',
    shortLabel: 'עלול להכיל',
    color: '#F59E0B',
    tint: 'rgba(245, 158, 11, 0.14)',
    hint: 'ייצור משותף או רכיב לא ברור — להימנע אלא אם נבדק.',
  },
  safe: {
    id: 'safe',
    label: 'נבדק ובטוח',
    shortLabel: 'בטוח',
    color: '#22C55E',
    tint: 'rgba(34, 197, 94, 0.14)',
    hint: 'נבדק מול התווית על ידי ההורים.',
  },
}

export const STATUS_ORDER = ['contains', 'may_contain', 'safe']

/** דיסקליימר — דרישה קשיחה, מוצג במסך פתיחה ובתחתית מסך התוצאות. */
export const DISCLAIMER =
  'כלי עזר בלבד — תמיד לבדוק את רשימת הרכיבים על האריזה. מתכונים משתנים.'
